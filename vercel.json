{
  "buildCommand": "node build.js",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/portal(.*)", "dest": "/portal/index.html" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
