{
  "name": "adroit-tech-services-root",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install-deps": "npm install --prefix adroit-tech-frontend && npm install --prefix adroit-tech-career",
    "build": "npm run install-deps && node build.js"
  }
}
